$(document).ready(function () {
  $('.about-us__slider').slick(
    {
      dots: true,
      accessibility: false,
      // autoplay: true,
      arrows: false,
      adaptiveHeight: true,
      // centerMode: true,
      // centerPadding: '60px',
    }
  );
});